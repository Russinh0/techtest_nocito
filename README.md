Levantar Cliente:
npm install && npm start

Levantar Servidor:

Revistar server/.env
DB_USERNAME= { TU USERNAME DE POSTGRESQL }
DB_PASSWORD= { TU PASSWORD DE POSTGRESQL }


Después correr:

npm install && npm start